# PLY UI 작업 지침

## 작업 전에 읽기

1. [디자인 원칙](docs/content/design-principles.mdx)을 읽는다. 이 파일이 원문이며, 다른 문서에 전체 내용을 복제하지 않는다.
2. [README](README.md)와 [개발 안내](docs/content/development.mdx)에서 현재 구조와 실행 방법을 확인한다. pnpm을 사용한다.
3. 변경 대상의 `docs/content/components/<name>.mdx`, 소스, 실행 예제, 관련 테스트를 읽는다. 반응형·조합 작업은 `docs/content/responsive.mdx`, 모션 작업은 `docs/content/motion.mdx`도 확인한다.

## 설계와 구현

- 안정성을 먼저 확보하고 명확성을 함께 충족하며, 모션으로 둘을 지원한다. 접근성·작업 완결성·사용자 통제권·오류 예방과 복구는 공통 조건이다.
- 같은 의미와 상황에 같은 규칙을 적용한다. 일관성을 모든 컴포넌트에 같은 크기나 효과를 적용하는 것으로 해석하지 않는다.
- 반응형은 모든 컴포넌트를 대상으로 한다. 화면 너비뿐 아니라 낮은 화면, 확대, 긴 콘텐츠, 가상 키보드, 입력 방식을 고려한다. 배치는 바꿀 수 있지만 작업 목적·정보·핵심 기능은 유지한다.
- 단독 예제의 외형만 고치지 않는다. 실제 조합, 기본값, 스타일 재정의, 상태 소유권과 의존성을 확인한다. 문서 전용 CSS로 라이브러리 결함을 가리지 않는다.
- Base UI의 의미 구조·키보드·포커스 동작을 보존하고 실제 조합에서 확인한다. 기반 라이브러리 사용만으로 접근성 검증이 끝났다고 판단하지 않는다.
- 새 컴포넌트와 변형은 구체적인 사용 목적이 있어야 한다. 기존 구성으로 해결 가능한지 먼저 확인한다.
- 확정된 결정·제안·알려진 문제를 구분한다. 근거 없는 지원 보장이나 기존 구현의 품질 선언을 하지 않는다.

## 검증과 보고

- 변경에 관련된 기존 검사를 실행한다. 동작 결함을 수정하면 재현 조건을 검증하고, 필요할 때 회귀 검사를 추가한다. 문서만 변경할 때는 관련 링크·MDX·타입 검증으로 범위를 맞춘다.
- 조합·반응형 변경은 문서 밖의 조합 및 관련 화면·테마·콘텐츠 조건을 확인한다. 패키지·스타일 배포에 영향을 주면 실제 설치 환경도 확인한다.
- 접근성·모션 변경은 키보드, 포커스 이동과 복귀, 상태 전달, 연속 조작, 모션 감소 중 관련 조건을 확인한다.
- 검증 명령과 대상은 개발 안내를 따른다. 지원 브라우저·기기·보조기기 중 확인하지 못한 환경은 미검증으로 보고한다.
- 결과에는 해결한 문제, 설계 이유, 기존 API·동작에 미치는 영향, 실행한 검사와 남은 한계를 설명한다. 자동 테스트 통과와 실제 사용자 검증을 구분한다.
- 복잡한 작업은 구조와 계획을 먼저 설명하고 작은 단계로 진행한다. 변경 후 핵심 로직과 diff를 설명한다.

`docs/engineering/history/`와 과거 감사 문서는 당시의 기록이다. 현재 원칙이나 지원 상태를 대신하지 않는다.
